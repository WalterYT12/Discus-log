// ============================================================================
// Your Firebase project config — fill this in ONCE, then leave it alone.
//
// This file is separate from index.html on purpose: whenever you get an
// updated index.html from Claude, you can drop it in without ever touching
// this file or losing your connection.
//
// Where to find these values: console.firebase.google.com -> your project ->
// Project settings (gear icon) -> scroll to "Your apps" -> the web app you
// created. If you already set up cloud sync before by pasting a config into
// the app itself, these are the exact same values — just copy them here
// instead.
//
// Once this is filled in, anyone who opens the app can tap "Sign in with
// Google" with their OWN Google account immediately — no setup, no pasting
// anything. Their data stays private to their own account either way.
// ============================================================================

window.DISCUS_FIREBASE_CONFIG = {
  apiKey: "AIzaSyALV7yurEjS0_Y8lPsm7HKW9FDeVERnUM0",
  authDomain: "discus-log.firebaseapp.com",
  projectId: "discus-log",
  storageBucket: "discus-log.firebasestorage.app",
  messagingSenderId: "985191541286",
  appId: "1:985191541286:web:a288a5a99fe937c851d4e7"
};
